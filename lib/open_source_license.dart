import 'package:flutter/material.dart';

class OpenSourceLicensePage extends StatelessWidget {
  const OpenSourceLicensePage();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('オープンソースライセンス'),
      ),
      body: const Text('ないよう'),
    );
  }
}
