name: qin_memo
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1
  hooks_riverpod: ^0.14.0
  flutter_hooks:
  json_annotation:
  freezed_annotation:
  http:
  intl:
  dio:
  fusionauth_dart_client: ^1.21.0
  smooth_page_indicator:
    git:
      url: https://github.com/Milad-Akarie/smooth_page_indicator.git
      ref: 0.3.0-nullSafety
dependency_overrides:
  json_annotation: 4.0.1

dev_dependencies:
  build_runner:
  freezed:
  json_serializable:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
